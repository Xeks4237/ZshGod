# Same function as prompt_zshgod_rectangular_username but shown only through ssh
function prompt_zshgod_rectangular_sshonly_username() {
	[[ -n $SSH_CONNECTION ]] && prompt_zshgod_rectangular_username
}
