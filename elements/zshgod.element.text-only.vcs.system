# Function which returns name of current vcs
function zshgod.element.text-only.vcs.system() {
	# Allow passing args to function for changing foreground color
	local fg_color=$1

	[[ -n $(zshgod.vcs.system) ]] || return
	print "%F{$fg_color}$(zshgod.vcs.system)%f"
}
