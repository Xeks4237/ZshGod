# Function which returns name of current vcs
function zshgod_text-only_vcs_system() {
	# Allow passing args to function for changing foreground color
	local fg_color=$1

	[[ -n $(zshgod_vcs_system) ]] || return
	print "%F{$fg_color}$(zshgod_vcs_system)%f"
}
