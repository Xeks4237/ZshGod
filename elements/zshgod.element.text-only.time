# Function for showing arrow with customly formatted current time
function zshgod.element.text-only.time() {
	# Allow passing args to function for changing foreground color
	local fg_color=$1

	print "%F{$fg_color}%D{%H:%M:%S}%f"
}
