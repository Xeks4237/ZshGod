# Same function as prompt_zshgod_right-to-left_hostname but shown only through ssh
function prompt_zshgod_right-to-left_sshonly_hostname() {
	[[ -n $SSH_CONNECTION ]] && prompt_zshgod_right-to-left_hostname
}
