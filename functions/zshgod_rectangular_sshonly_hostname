# Same function as zshgod_rectangular_hostname but shown only through ssh
function zshgod_rectangular_sshonly_hostname() {
	# Allow passing args to function for changing foreground and background colors
	local bg_color=$1
	local fg_color=$2

	[[ -n $SSH_CONNECTION ]] && zshgod_rectangular_hostname $bg_color $fg_color
}
