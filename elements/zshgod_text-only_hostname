# Function which returns colored arrow with current hostname
function zshgod_text-only_hostname() {
	# Allow passing args to function for changing foreground color
	local fg_color=$1

	print "%F{$fg_color}%m%f"
}
