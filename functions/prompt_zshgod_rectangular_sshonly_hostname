# Same function as prompt_zshgod_rectangular_hostname but shown only through ssh
function prompt_zshgod_rectangular_sshonly_hostname() {
	[[ -n $SSH_CONNECTION ]] && prompt_zshgod_rectangular_hostname
}
