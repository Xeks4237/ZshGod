# Function which returns current amount of jobs if there at least 1 job
# Uses builtin %j to get number of jobs
function zshgod_text-only_jobs() {
	# Allow passing args to function for changing foreground color
	local fg_color=$1

	print "%(1j,%F{$fg_color}jobs: [%j]%f,)"
}
